import java.time.LocalDate;

public class HotesseAir extends Equipage {

    // Constructeur
    public HotesseAir(String nom, String prenom, LocalDate dateNaissance, int idPers, String job) {
        super(nom, prenom, dateNaissance, idPers, job);
    }

}
