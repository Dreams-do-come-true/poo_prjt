import java.time.LocalDate;

public class Stewart extends Equipage {

    // Constructeur
    public Stewart(String nom, String prenom, LocalDate dateNaissance, int idPers, String job) {
        super(nom, prenom, dateNaissance, idPers, job);
    }

}
